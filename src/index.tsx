// Se agrega este archivo para que genere el index.css en el dist
// y así pueda ser utilizado por storybook
import './assets/scss/dev.scss'
import Button, { ButtonProps } from './components/Button'
import Icon, { IconProps } from './components/Icon'

export { Button, ButtonProps, Icon, IconProps }
